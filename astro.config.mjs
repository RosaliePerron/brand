// @ts-check
import { defineConfig } from 'astro/config';

// https://astro.build/config
export default defineConfig({
  // Update `site` to your real domain once you deploy (used for canonical URLs / future RSS).
  site: 'https://example.com',
});
